# -*- coding: utf-8 -*-
"""
量化交易系统 - 配置文件
====================================
这是整个系统的"控制面板"，所有重要参数都在这里修改。
零基础小白只需要修改这个文件就能调整系统行为。
"""

# ============================================================
# 第一部分：iFinD 数据源配置
# ============================================================
# iFinD 是同花顺提供的金融数据服务，可以获取真实股票行情数据。
#
# 如何获取 Token：
#   1. 访问 https://mcp.51ifind.com/
#   2. 注册/登录后，在"个人中心 -> 密钥"页面获取你的 API Key
#   3. 将获取的 Key 粘贴到下面的 IFIND_AUTH_TOKEN 引号中
#
# 如果暂时没有 Token，系统会自动使用"模拟数据模式"运行，
# 你依然可以体验完整的回测流程！

IFIND_AUTH_TOKEN = ""  # 在引号中填入你的 iFinD API Key，例如 "abc123xyz"

# iFinD API 服务地址（一般不需要修改）
IFIND_API_BASE = "https://api-mcp.51ifind.com:8643/ds-mcp-servers"


# ============================================================
# 第二部分：数据获取配置
# ============================================================
# 股票代码：A 股股票代码，例如 "贵州茅台" 或 "600519.SH"
STOCK_CODE = "贵州茅台"

# 回测时间范围
START_DATE = "2024-01-01"  # 开始日期，格式：年-月-日
END_DATE = "2025-12-31"    # 结束日期，格式：年-月-日

# 数据模式：
#   "ifind"  —— 使用 iFinD 获取真实股票数据（需要 Token）
#   "mock"   —— 使用模拟数据（无需 Token，适合学习和测试）
DATA_MODE = "ifind" if IFIND_AUTH_TOKEN else "mock"


# ============================================================
# 第三部分：交易策略参数
# ============================================================
# 双均线策略参数
# 原理：短期均线上穿长期均线时买入（金叉），下穿时卖出（死叉）
SHORT_MA_PERIOD = 5    # 短期均线天数（常用：5日、10日）
LONG_MA_PERIOD = 20    # 长期均线天数（常用：20日、60日）


# ============================================================
# 第四部分：回测配置
# ============================================================
INITIAL_CAPITAL = 100000.0  # 初始资金（元），默认 10 万元
COMMISSION_RATE = 0.0003    # 交易手续费率（万分之三），买卖各收一次
STAMP_TAX_RATE = 0.001      # 印花税率（千分之一），仅卖出时收取
SLIPPAGE = 0.001            # 滑点（千分之一），模拟实际成交价与预期价的偏差


# ============================================================
# 第五部分：可视化配置
# ============================================================
# 图表显示设置
FIGURE_SIZE = (14, 9)       # 图表大小（宽, 高）
FIGURE_DPI = 100            # 图表清晰度
SAVE_CHART = True           # 是否将回测图表保存为图片
CHART_FILENAME = "backtest_result.png"  # 图表保存文件名

# 中文字体设置（Windows 系统一般不需要修改）
CHINESE_FONT = "SimHei"     # 黑体，用于显示中文


# ============================================================
# 第六部分：高级配置（一般不需要修改）
# ============================================================
# iFinD 各服务端点
IFIND_SERVERS = {
    "stock": f"{IFIND_API_BASE}/hexin-ifind-ds-stock-mcp",
    "fund": f"{IFIND_API_BASE}/hexin-ifind-ds-fund-mcp",
    "index": f"{IFIND_API_BASE}/hexin-ifind-ds-index-mcp",
    "edb": f"{IFIND_API_BASE}/hexin-ifind-ds-edb-mcp",
    "news": f"{IFIND_API_BASE}/hexin-ifind-ds-news-mcp",
    "bond": f"{IFIND_API_BASE}/hexin-ifind-ds-bond-mcp",
    "global_stock": f"{IFIND_API_BASE}/hexin-ifind-ds-global-stock-mcp",
}

# 数据缓存目录（缓存已下载的数据，避免重复请求）
CACHE_DIR = "data_cache"
